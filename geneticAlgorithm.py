class GeneticAlgorithm:
	def __init__(self, initialPopulation, generations):
		self.population = initialPopulation
		self.generations = generations

	def fitness(self):
		score = 0
		
		return
  	def crossover(self, outro):
		return
 	def mutate(self):
		return
	def letsBora(self):

		best = [float('-inf'), None]
		for _0 in range(self.generations):
			

		return
